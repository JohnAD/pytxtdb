#!/bin/bash
# [ -x deactivate ] && echo "Not inside a virtualenv. If it exists already, run 'source venv/bin/activate'. Otherwise create the virtualenv." >&2 && exit 1
cp ~/Projects/libtxtdb/src/libtxtdb.so ./src/
# source venv/bin/activate
# pip3 uninstall txtdb
# python -m pip install -e ./
